# ⚙️ Настройка АВТОМАТИЧЕСКОЙ загрузки на GitHub

## 🚀 Быстрая настройка (один раз)

### 1. Откройте PowerShell от имени администратора
- Правой кнопкой на PowerShell → **Запуск от имени администратора**

### 2. Выполните команду:
```powershell
cd "d:\! Погода\MIRA 0.2 (небосвод)"
.\setup-auto-git.ps1
```

### 3. Готово!
Теперь **каждое изменение файла** автоматически загружается на GitHub через **5 секунд**.

---

## 📊 Как это работает

```
1. Вы изменяете файл (например, index.html)
   ↓
2. Скрипт замечает изменение
   ↓
3. Ждёт 5 секунд (вдруг вы ещё редактируете)
   ↓
4. git add . + git commit + git push
   ↓
5. Через 1-2 минуты на GitHub Pages
```

---

## 🔍 Просмотр статуса

### Лог авто-загрузки:
```powershell
Get-Content ".auto-git-watch.log" -Tail 50 -Wait
```

### Задачи планировщика:
```powershell
Get-ScheduledTask -TaskName "MIRA Auto-Git-Push"
```

### Последние коммиты:
Откройте: https://github.com/kkav45/mira/commits

---

## 🛑 Отключение

### Временно остановить:
```powershell
# Найти процесс PowerShell с скриптом
Get-Process powershell | Where-Object {$_.CommandLine -like "*auto-git-watch*"}

# Остановить
Stop-Process -Name powershell -Force
```

### Полностью отключить:
```powershell
# От имени администратора
Unregister-ScheduledTask -TaskName "MIRA Auto-Git-Push" -Confirm:$false
```

---

## 📋 Параметры

| Параметр | Значение |
|----------|----------|
| **Задержка** | 5 секунд после изменения |
| **Интервал** | Минимум 2 сек между загрузками |
| **Исключения** | .git, node_modules, .vscode |
| **Формат коммита** | `Auto-save: yyyy-MM-dd HH:mm:ss` |
| **Логирование** | `.auto-git-watch.log` |

---

## ✅ Проверка работы

1. **Измените любой файл** (добавьте пробел в index.html)
2. **Сохраните**
3. **Подождите 10 секунд**
4. **Проверьте лог:**
   ```powershell
   Get-Content ".auto-git-watch.log" -Tail 20
   ```
5. **Проверьте GitHub:** https://github.com/kkav45/mira/commits

---

## 🔧 Решение проблем

### Ошибка: "Task not found"
→ Запустите `.\setup-auto-git.ps1` от имени администратора

### Ошибка: "Git not found"
→ Установите Git: https://git-scm.com/

### Ошибка: "Permission denied"
→ Проверьте токен GitHub в Windows Credentials Manager

### Файлы не загружаются
→ Проверьте лог: `.auto-git-watch.log`

---

## 📞 Файлы системы

| Файл | Назначение |
|------|------------|
| **auto-git-watch.ps1** | Основной скрипт мониторинга |
| **setup-auto-git.ps1** | Настройка автозапуска |
| **.auto-git-watch.log** | Лог загрузок |
| **AUTO_GIT_SETUP.md** | Эта инструкция |

---

**Версия:** 1.0  
**Дата:** Февраль 2026  
**Репозиторий:** https://github.com/kkav45/mira
